<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyFirstAppMobile.EntryPage"
             Title="EntryPage">
    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="Nouvelle Activité" FontSize="24" FontAttributes="Bold" />

        <Entry Placeholder="Type d'activité (ex: Course)" 
           Text="{Binding ActivityType, Mode=TwoWay}" />

        <Entry Placeholder="Durée (minutes)" 
           Text="{Binding DurationMinutes, Mode=TwoWay}" 
           Keyboard="Numeric" />

        <Editor Placeholder="Notes additionnelles" 
            Text="{Binding Notes, Mode=TwoWay}"
            HeightRequest="100" />

        <Button Text="Enregistrer" 
            Command="{Binding SaveCommand}"
            BackgroundColor="Green"
            TextColor="White" />

        <Button Text="Annuler" 
            Command="{Binding CancelCommand}"
            BackgroundColor="Gray" />
    </VerticalStackLayout>
</ContentPage>